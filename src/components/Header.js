import React from 'react';
import Navbar from '../navigation/Navbar';

const Header = () => {
    return (
        <div id="header">
            <Navbar />
        </div>
    );
}

export default Header;